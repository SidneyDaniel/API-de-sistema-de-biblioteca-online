export interface docRefId{
    id: string
}
  
export interface collectionItem {
    docName: string;
    accessCode: string;
}

export interface Livro {
    name: string;
    author: string;
    publisher: string;
    pages: number;
    cover: string;
    readLink: string;
}

